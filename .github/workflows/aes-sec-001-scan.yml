name: AES-SEC-001 Scan

on:
  pull_request:
    paths:
      - "**/*.c"
      - "**/*.cc"
      - "**/*.cpp"
      - "**/*.cxx"
      - "**/*.h"
      - "**/*.hh"
      - "**/*.hpp"
      - "**/*.hxx"
      - "**/*.m"
      - "**/*.mm"
      - "**/*.S"
      - "**/*.s"
      - "**/*.asm"
      - "**/*.inc"
      - "Makefile"
      - "**/Makefile"
      - "**/*.mk"
      - "**/CMakeLists.txt"
      - "**/*.cmake"
      - "**/meson.build"
      - "docs/engineering/SECURE-C-CXX.md"
      - "docs/engineering/AES-SEC-001-waivers.md"
      - "docs/engineering/AES-SEC-001-enforcement-plan.md"
      - "scripts/aes_sec_001_scan.py"
      - "scripts/aes_sec_001_aggregate.py"
      - "config/aes-sec-001-repositories.json"
      - ".github/workflows/aes-sec-001-scan.yml"
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      include_dangerous_primitives:
        description: "Also report review-required dangerous primitives such as memcpy/malloc/free/snprintf."
        required: false
        default: "false"
        type: choice
        options:
          - "false"
          - "true"
      strict:
        description: "Fail the workflow when the secure profile is missing or banned APIs are found."
        required: false
        default: "true"
        type: choice
        options:
          - "true"
          - "false"
      ecosystem_scan:
        description: "Also scan all repositories listed in config/aes-sec-001-repositories.json."
        required: false
        default: "false"
        type: choice
        options:
          - "false"
          - "true"
      include_third_party:
        description: "Include third-party mirror/fork repositories in the ecosystem scan."
        required: false
        default: "false"
        type: choice
        options:
          - "false"
          - "true"

permissions:
  contents: read

defaults:
  run:
    shell: bash

jobs:
  scan-local-repository:
    name: Scan local repository
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Show scanner help
        run: python3 scripts/aes_sec_001_scan.py --help

      - name: Run AES-SEC-001 scanner as JSON
        run: |
          EXTRA_ARGS=()
          if [[ "${{ github.event.inputs.include_dangerous_primitives || 'false' }}" == "true" ]]; then
            EXTRA_ARGS+=("--include-dangerous-primitives")
          fi

          python3 scripts/aes_sec_001_scan.py . \
            --repo-name "${{ github.repository }}" \
            --format json \
            "${EXTRA_ARGS[@]}" \
            > aes-sec-001-scan.json

      - name: Run AES-SEC-001 scanner as Markdown
        run: |
          EXTRA_ARGS=()
          if [[ "${{ github.event.inputs.include_dangerous_primitives || 'false' }}" == "true" ]]; then
            EXTRA_ARGS+=("--include-dangerous-primitives")
          fi

          python3 scripts/aes_sec_001_scan.py . \
            --repo-name "${{ github.repository }}" \
            --format markdown \
            "${EXTRA_ARGS[@]}" \
            > aes-sec-001-scan.md

      - name: Add report to job summary
        run: cat aes-sec-001-scan.md >> "$GITHUB_STEP_SUMMARY"

      - name: Enforce strict gate
        if: ${{ github.event_name != 'workflow_dispatch' || github.event.inputs.strict == 'true' }}
        run: |
          EXTRA_ARGS=()
          if [[ "${{ github.event.inputs.include_dangerous_primitives || 'false' }}" == "true" ]]; then
            EXTRA_ARGS+=("--include-dangerous-primitives")
          fi

          python3 scripts/aes_sec_001_scan.py . \
            --repo-name "${{ github.repository }}" \
            --strict \
            "${EXTRA_ARGS[@]}"

      - name: Upload scan reports
        uses: actions/upload-artifact@v4
        with:
          name: aes-sec-001-scan-${{ github.run_id }}
          path: |
            aes-sec-001-scan.json
            aes-sec-001-scan.md
          if-no-files-found: error

  scan-ecosystem:
    name: Scan ecosystem manifest
    runs-on: ubuntu-latest
    if: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.ecosystem_scan == 'true' }}

    steps:
      - name: Check out AEMS
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Show aggregate runner help
        run: python3 scripts/aes_sec_001_aggregate.py --help

      - name: Run ecosystem scan as JSON
        run: |
          EXTRA_ARGS=()
          if [[ "${{ github.event.inputs.include_dangerous_primitives || 'false' }}" == "true" ]]; then
            EXTRA_ARGS+=("--include-dangerous-primitives")
          fi
          if [[ "${{ github.event.inputs.include_third_party || 'false' }}" == "true" ]]; then
            EXTRA_ARGS+=("--include-third-party")
          fi

          python3 scripts/aes_sec_001_aggregate.py \
            --manifest config/aes-sec-001-repositories.json \
            --work-dir "$RUNNER_TEMP/aes-sec-001-checkouts" \
            --format json \
            "${EXTRA_ARGS[@]}" \
            > aes-sec-001-ecosystem.json

      - name: Run ecosystem scan as Markdown
        run: |
          EXTRA_ARGS=()
          if [[ "${{ github.event.inputs.include_dangerous_primitives || 'false' }}" == "true" ]]; then
            EXTRA_ARGS+=("--include-dangerous-primitives")
          fi
          if [[ "${{ github.event.inputs.include_third_party || 'false' }}" == "true" ]]; then
            EXTRA_ARGS+=("--include-third-party")
          fi

          python3 scripts/aes_sec_001_aggregate.py \
            --manifest config/aes-sec-001-repositories.json \
            --work-dir "$RUNNER_TEMP/aes-sec-001-checkouts" \
            --format markdown \
            "${EXTRA_ARGS[@]}" \
            > aes-sec-001-ecosystem.md

      - name: Add ecosystem report to job summary
        run: cat aes-sec-001-ecosystem.md >> "$GITHUB_STEP_SUMMARY"

      - name: Enforce ecosystem strict gate
        if: ${{ github.event.inputs.strict == 'true' }}
        run: |
          EXTRA_ARGS=()
          if [[ "${{ github.event.inputs.include_dangerous_primitives || 'false' }}" == "true" ]]; then
            EXTRA_ARGS+=("--include-dangerous-primitives")
          fi
          if [[ "${{ github.event.inputs.include_third_party || 'false' }}" == "true" ]]; then
            EXTRA_ARGS+=("--include-third-party")
          fi

          python3 scripts/aes_sec_001_aggregate.py \
            --manifest config/aes-sec-001-repositories.json \
            --work-dir "$RUNNER_TEMP/aes-sec-001-checkouts" \
            --strict \
            "${EXTRA_ARGS[@]}"

      - name: Upload ecosystem reports
        uses: actions/upload-artifact@v4
        with:
          name: aes-sec-001-ecosystem-${{ github.run_id }}
          path: |
            aes-sec-001-ecosystem.json
            aes-sec-001-ecosystem.md
          if-no-files-found: error
